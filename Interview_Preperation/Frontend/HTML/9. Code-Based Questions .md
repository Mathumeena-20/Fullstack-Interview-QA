# ✅ **1. Responsive Card Component (HTML Structure Only)**

(Semantic, flexible, mobile-friendly structure)

### ✔ HTML:

```html
<article class="card">
  <figure class="card-media">
    <img src="product.jpg" alt="Product Image">
  </figure>

  <header class="card-header">
    <h2 class="card-title">Product Title</h2>
  </header>

  <p class="card-description">
    Short description of the product goes here.
  </p>

  <footer class="card-footer">
    <button type="button">View Details</button>
  </footer>
</article>
```

### ✔ Explanation:

* `<article>` → card can stand alone
* `<figure>` → good for images/media
* `<header>` → card title
* `<footer>` → actions (button, link)
* No fixed widths → responsive by design

---

# ✅ **2. Login Form (Email + Password + Remember Me + Submit)**

### ✔ HTML:

```html
<form action="/login" method="POST">
  
  <label for="email">Email</label>
  <input id="email" type="email" name="email" required>

  <label for="password">Password</label>
  <input id="password" type="password" name="password" required>

  <div>
    <input id="remember" type="checkbox" name="remember">
    <label for="remember">Remember me</label>
  </div>

  <button type="submit">Login</button>

</form>
```

### ✔ Explanation:

* All fields use `<label>` → improves accessibility
* `required` → HTML5 validation
* Grouped remember me checkbox

---

# ✅ **3. Accessible Button (Keyboard + Screen Reader Friendly)**

### ✔ HTML:

```html
<button type="button" aria-label="Open menu">
  <span aria-hidden="true">☰</span>
</button>
```

### ✔ Why this is accessible?

* `<button>` is naturally keyboard-focusable
* Works with **Enter/Space keys**
* `aria-label="Open menu"` → screen readers read meaningful text
* `aria-hidden="true"` hides the visual icon from screen readers

👉 Interview-safe line:

> Always use `<button>`, not `<div>` or `<span>`, for clickable actions.

---

# ✅ **4. Responsive Images Using `<picture>`**

### ✔ HTML:

```html
<picture>
  <source srcset="banner-large.jpg" media="(min-width: 800px)">
  <source srcset="banner-medium.jpg" media="(min-width: 500px)">
  <img src="banner-small.jpg" alt="Promotional banner">
</picture>
```

### ✔ Explanation:

* `<source>` elements load **different images based on screen size**
* `<img>` is the fallback
* Saves bandwidth on mobile
* Perfect for hero banners, product images

---

# ✅ **5. Table With Header, Body, Footer**

### ✔ HTML:

```html
<table>
  <thead>
    <tr>
      <th>Product</th>
      <th>Price</th>
      <th>Status</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>Laptop</td>
      <td>$999</td>
      <td>In Stock</td>
    </tr>
    <tr>
      <td>Mouse</td>
      <td>$25</td>
      <td>Out of Stock</td>
    </tr>
  </tbody>

  <tfoot>
    <tr>
      <td colspan="3">Total Items: 2</td>
    </tr>
  </tfoot>
</table>
```

### ✔ Explanation:

* `<thead>` → column headers for accessibility
* `<tbody>` → main data
* `<tfoot>` → summary/footer row
* Screen readers announce headers properly

---


Just say **“Next tasks”**.
